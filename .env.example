# Copy this file to ".env.local" and fill in your keys.
# The app runs fully in "Practice Mode" without any keys, so you can try it first.

# --- Writing feedback & turning stories into scenes (OpenAI) ---
OPENAI_API_KEY=
# Optional: which text model to use (default: gpt-4o-mini)
OPENAI_TEXT_MODEL=gpt-4o-mini

# --- Turning scenes into images & videos (Replicate) ---
REPLICATE_API_TOKEN=
# Optional: any text-to-image model on Replicate for the storyboard previews
# (default: black-forest-labs/flux-schnell -- fast & cheap, great for iterating)
REPLICATE_IMAGE_MODEL=black-forest-labs/flux-schnell
# Optional: any text-to-video model on Replicate (default: wan-video/wan-2.2-t2v-fast)
REPLICATE_VIDEO_MODEL=wan-video/wan-2.2-t2v-fast

# --- Character / art-style consistency ---
# The app always builds a "story bible" (one art style + locked character looks)
# and injects it into every image and video prompt, plus reuses one image seed.
# For the STRONGEST storyboard<->video match, switch to an image-to-video model
# that animates the approved storyboard image as its first frame:
#   REPLICATE_VIDEO_MODE=i2v
#   REPLICATE_VIDEO_MODEL=<an image-to-video model, e.g. wan-video/wan-2.2-i2v-fast>
# If your i2v model names its image input differently, set the key here:
#   REPLICATE_VIDEO_IMAGE_KEY=image
REPLICATE_VIDEO_MODE=t2v
REPLICATE_VIDEO_IMAGE_KEY=image
